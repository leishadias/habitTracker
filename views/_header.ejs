<nav class="navbar navbar-expand-lg sticky-top">
    <div class="container-fluid d-flex">
      <!--icon-->
      <a class="navbar-brand text-white">
        <img src="/images/logo.png" alt="Bootstrap" width="50" height="50">
        Habit Tracker
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <!--if user has logged in-->
        <% if (locals.user){ %>
          <div id="add-habit-btn" data-bs-toggle="modal" data-bs-target="#addItemForm" class="nav-item">
            Add new habit
          </div>
          <div class="nav-item dropdown d-flex">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= user.name%>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li><a class="dropdown-item" href="/users/sign-out">Sign out <i class="fa-solid fa-right-from-bracket"></i></a></li>
            </ul>
          </div>
        <%} else { %>
            <ul class="nav justify-content-end">
              <li class="nav-item">
                <a class="nav-option" href="/users/sign-in"> Sign In </a>
              </li>
              <li class="nav-item">
                <a class="nav-option" href="/users/sign-up"> Sign Up </a>
              </li>
            </ul>
        <%}%> 
      </div>
    </div>
</nav>